configurations:
  - transformer.yaml
resources:
  - gitrepository.yaml
  - helmrelease.yaml

configMapGenerator:
  - name: arkime-ek-values
    namespace: arkime-ek
    behavior: create
    files:
    - values.yaml=values.yaml
